name: ocforge_gui
description: "Material 3 Expressive desktop front-end for the ocforge OpenCore EFI builder."
publish_to: "none"
version: 0.4.0+5

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=3.0.0 <6.0.0"

flutter:
  uses-material-design: true
